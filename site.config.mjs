/** Canonical origin + path. astro.config.mjs and check-base-build.mjs both import this. Cutover flips SITE and BASE together. */
export const SITE = 'https://kalpit.me'
export const BASE = '/switch-karle'
