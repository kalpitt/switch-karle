/** Date the statutory constants in `src/engine/` were last checked against a primary source. */
export const RULES_LAST_VERIFIED = '2026-08-23'

/** Union Budget season: re-open every constant in February and bump this date. The footer chip is the ritual. */
