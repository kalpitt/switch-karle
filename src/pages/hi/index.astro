---
import Base from '../../layouts/Base.astro'
import HomeTool from '../../tools/home/index'

const title = 'स्विच कर ले — पूरा job switch, “बस, अब यहाँ नहीं” से day 90 तक'
const description =
  '“बस, अब यहाँ नहीं” से लेकर नई नौकरी के पहले 90 दिन तक: आपकी exit dates, offer में असल में क्या मिलेगा, notice और F&F. Free, और पूरा आपके browser में.'
const site = (import.meta.env.SITE || 'https://kalpit.me').replace(/\/$/, '')
const base = (import.meta.env.BASE_URL || '/').endsWith('/')
  ? import.meta.env.BASE_URL
  : `${import.meta.env.BASE_URL}/`
const url = `${site}${base}hi/`
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebApplication',
  name: 'स्विच कर ले',
  url,
  applicationCategory: 'FinanceApplication',
  operatingSystem: 'Any',
  offers: { '@type': 'Offer', price: '0', priceCurrency: 'INR' },
  description,
}
---

<Base title={title} description={description} jsonLd={jsonLd} lang="hi">
  <HomeTool client:load lang="hi" />
</Base>
